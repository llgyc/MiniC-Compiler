int n = 3;
void toh(int no, int src, int dst, int spare) {
	if (no == 1) {
		putch(src);
		putch(dst);
		putch(10);
		return;
	}
	toh(no-1,src, spare, dst);
	toh(no-1,spare,dst,src);
}
int main() {
	toh(3, 65, 66, 67);
	return 0;
}
	
